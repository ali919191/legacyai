# AI Models

This folder is intended to store trained models, checkpoints, or exported artifacts.

Since Git does not track empty directories, you can add placeholder files here or populate it with your model outputs.
